<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cqs.demo.models.unit.strategy.dao.StrategyDao">

    <select id="findAll" resultType="Strategy">
        SELECT *
        FROM strategy
        <!--limit #{offset},#{limit} -->
    </select>


    <select id="findById" parameterType="int" resultType="Strategy">
        SELECT *
        FROM strategy t
        where t.id = #{id}
    </select>


    <insert id="insert" parameterType="Strategy">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Thu Nov 24 22:29:57 CST 2016.
        -->
        insert into strategy (id, user_id, in_traffic_max,
        in_traffic_guarantee, out_traffic_max, out_traffic_guarantee,
        create_date, update_date)
        values (#{id,jdbcType=INTEGER}, #{userId,jdbcType=INTEGER}, #{inTrafficMax,jdbcType=REAL},
        #{inTrafficGuarantee,jdbcType=REAL}, #{outTrafficMax,jdbcType=REAL}, #{outTrafficGuarantee,jdbcType=REAL},
        #{createDate,jdbcType=TIMESTAMP}, #{updateDate,jdbcType=TIMESTAMP})
    </insert>

    <insert id="batchInsert" parameterType="Strategy">
        INSERT INTO strategy (id, user_id, in_traffic_max,
        in_traffic_guarantee, out_traffic_max, out_traffic_guarantee,
        create_date, update_date)
        VALUES
        <foreach collection="list" item="strategy" separator=",">
            (#{strategy.id,jdbcType=INTEGER}, #{strategy.userId,jdbcType=INTEGER},
            #{strategy.inTrafficMax,jdbcType=REAL},
            #{strategy.inTrafficGuarantee,jdbcType=REAL}, #{strategy.outTrafficMax,jdbcType=REAL},
            #{strategy.outTrafficGuarantee,jdbcType=REAL},
            #{strategy.createDate,jdbcType=TIMESTAMP}, #{strategy.updateDate,jdbcType=TIMESTAMP})
        </foreach>
    </insert>


</mapper>